{% form_theme form 'bootstrap_4_layout.html.twig' %}

{{ form_start(form) }}
    {{ form_widget(form) }}
    <div class="card-footer">
                <button type="submit" class="btn btn-block btn-primary">Enregistre</button>
            </div>
    
{{ form_end(form) }}
