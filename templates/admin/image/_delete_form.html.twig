<form method="post" action="{{ path('admin_image_delete', {'id': image.id}) }}" onsubmit="return confirm('Êtes-vous sûr de bien vouloir supprimer cet élément?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ image.id) }}">
    <button class="btn bnt-block btn-danger">Supprimer</button>
</form>
